# --- Python Standard ---
venv/
env/
.venv/
__pycache__/
*.py[cod]
*.so

# --- Model Artifacts (Too large for Git) ---
# We ignore the .pth files, but we keep the empty 'model/' folder
model/*.pth
*.pth

# --- Data & Logs ---
# Ignore the actual downloaded images (save space)
resources/Faces/
resources/Celebrity Faces Dataset/

# Ignore the CSV splits (they contain absolute paths specific to YOUR PC)
training/data_splits/

# Ignore Kaggle download cache if local
.kaggle/

# --- Logs & OS Junk ---
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Temporary Test Images ---
test_images/*
# (Optional) Keep one placeholder if you want the folder to exist
!test_images/.gitkeep
